export interface PedidoCompraItens {
    id?: number; // Identificador único do item (opcional, gerado pelo sistema)
    produto: string; // Nome do produto
    quantidade: number; // Quantidade solicitada
  }
  